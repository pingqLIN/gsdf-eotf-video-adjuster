# LumaLift 擴充介面設計參考

本檔是 Chrome 擴充控制面的專案設計來源，定義介面視覺語彙，以及外部參考與專案需求之間的邊界。

## 設計角色

LumaLift 是精簡的專業影片控制面板，不是 landing page。介面應該像可反覆使用的精密檢查儀表：資訊密度高、安靜、可讀，而且可信任。

首屏優先順序是操作：

- 啟用或旁路 EOTF/GSDF 效果
- 調整目標亮度、Gamma 補償與 filter amount
- 依實際執行順序沿著同一條可見訊號鏈操作：細節整形、感知轉換、Levels、色彩，最後 Dither
- 在可逆的診斷分支預覽並明確套用硬 8-bit JND 階數重新分配，且不重設其他影像設定
- 擴充程式預設從瀏覽器原生側邊欄開啟，頁面內浮動面板只保留為相容性備援
- 在唯一的捲動區上方固定參考帶，可直接切換即時輸出曲線與精簡硬 8-bit JND 比較
- 將完整 Dither 控制直接放入終端量化階段，不再透過額外參考分欄開啟

新版主面板刻意不提供相機亮度估算與元素選取。Web 相機亮度偵測只保留在獨立路由，作為粗略視覺 meter；擴充套件則在主要控制面以外保留 target-picker 相容能力。

## 參考邊界

Stitch 匯出內容只能作為視覺語彙參考，不是產品規格。

可以參考 Stitch 的部分：

- 字級密度與數值讀out層級
- 內凹 wells、細線邊框與上緣高光
- 緊湊的圖示按鈕與共用滑桿／加減控制骨架
- 深色儀表面板的對比節奏
- 標籤、滑桿、狀態列的間距節奏

不可採用 Stitch 的部分：

- 功能名稱或工作流程需求
- 圖表資料、公式行為或 GSDF 模型解讀
- 診斷圖樣內容
- 截圖影像作為正確性參考
- Stitch 裡的參考/測試圖；它不是本專案的連續頻率解析度圖

目前專案實作才是 GSDF-QC 圖樣的權威來源。連續頻率解析度、調變、線對、灰階與漸層檢查都由本機 canvas 程式產生，不得以 Stitch 錯誤參考圖取代。

## 視覺語彙

面板採用 **Signal Path Console（訊號鏈控制台）** 視覺系統。左側訊號脊柱與編號階段直接呈現處理順序，不再使用 Basic／Advanced 卡片牆：

- 使用高對比的中性深色／明亮 chrome，而不是裝飾性玻璃效果
- 以 4px 為基礎的緊湊間距，同時保持可讀性
- 控制項 4px 圓角、外殼 8px 圓角；除了狀態指示外避免膠囊形控制
- 2px 表示訊號路徑、1px 表示功能分隔，並以三階清楚表面建立層次
- 效果旁路時不得降低整區透明度；設定仍需清楚可讀並持續呈現即時預覽
- 表面維持克制中性色，低彩度青色只用於焦點、訊號流與可操作狀態

字體分工：

- Bahnschrift 用於標籤、標題與緊湊 UI 控制
- Cascadia Mono 用於數值、單位、圖例與診斷標籤

互動控制的字距應保持克制。寬字距只保留給小型儀器標籤，不用於內文。

## 互動規則

純圖示按鈕必須保持方形、尺寸穩定，並透過 `title` 或 `aria-label` 說明用途。

主要流程只使用兩種可對齊的控制骨架。連續值使用滑桿；離散數值與依序切換的目標項目使用「減／目前值／加」控制。Formula、Pipeline 與 Gamut 因此沿用數字加減控制的結構來替換目前目標，不再引入第三種分段按鈕樣式。

滑桿應像實體 fader：

- 6px 內凹軌道
- 矩形 thumb
- 需要時提供清楚 min/max 標籤
- 固定數值欄寬，避免數值更新造成版面跳動

輸出的黑點與白點共用同一條 0–256 軸。兩個控制柄分別位於軸的左右端、不得交叉，並同時顯示兩端數值與剩餘 tone 數。

端點可以使用完整軸域：黑點可到 255，白點可到 1。唯一不變條件是至少保留 1 code 間距，因此兩個控制柄相遇時停止，兩端合計最多可裁切 255 codes。

細節與中等尺度銳利化刻意放在所選 GSDF／CSDF 轉換之前，讓轉換、輸出 Levels 與色彩階段在終端 Dither 前約束銳利化結果。若把銳利化移到轉換之後，局部對比會更直接，但也會在感知重映射後重新引入 overshoot 與 halo，因此不採用為預設訊號順序。

Filter amount 是完整轉換區塊的混合控制，不是與上方效果並列的孤立功能。共用括線與接點必須包住目標亮度、Gamma 補償、Formula、Pipeline 與 Gamut，最後連到 Filter amount。

固定頂部只保留一個權威啟用表面。啟用時，整個 `ACTIVE` 控制套用綠色狀態底色；右側不得再以第二個 route／status 區塊重複同一個 ON 狀態。

整個不捲動的頂部區域是一體化的緊湊儀表頭。品牌、工具、啟用、重設、三個檢視啟動鍵、參考切換與目前參考內容共用同一個連續表面，不再呈現為分離的 header 與曲線卡；`ACTIVE` 與等寬的 GSDF、CSDF、連續漸層啟動鍵位於同一列，取代原本的訊號路徑摘要文字，並以獨立瀏覽器彈出視窗開啟既有完整檢視，不切換側邊欄目前的工作流程。

固定儀表頭的高度隨 viewport 動態調整。曲線模式必須以完整的 flex 高度鏈一路延伸到 chart viewport，讓既有 `ResizeObserver` 使用所有剩餘空間調整繪圖高度，而不是固定為 150px。

完整 GSDF-QC 視圖必須保持內容正確。視覺打磨可以調整框架、標籤與控制外觀，但圖樣結構仍以本專案為準。

置頂參考帶中的精簡硬 8-bit JND 比較屬於診斷參考。主訊號鏈把 JND 最佳化呈現為附掛的診斷分支，而不是序列處理階段。它可以明確套用受限重新分配，但必須保持可見且可逆，並且不得重設 Lmax、Gamma、色域、路徑或其他影像控制。兩個介面都必須同時顯示保留之非零階差均勻度與包含合併階差的全部輸入結果；不得在未揭露合併數量時，只宣稱較低的非零階差標準差。

## 品質檢查

UI 變更完成前：

- 執行 extension 的 TypeScript/build 檢查
- 用實際瀏覽器確認渲染結果，不只看程式碼
- 若變更影響面板結構，檢查 Signal Path Console 每個階段、JND 診斷分支、置頂曲線／JND 切換，以及原生側邊欄版面
- 若變更獨立路由的相機亮度偵測，必須保持資料本機處理，並標示為粗略估計，不得暗示 colorimeter、DICOM 或 CSDF 驗證
- 確認精簡寬度下文字不會與控制項碰撞
- 確認深色與淺色主題的按鈕邊界都清楚可見
